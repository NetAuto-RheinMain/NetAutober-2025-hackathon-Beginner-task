{
    "name": "SR Linux Beginner Lab",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "customizations": {
      "vscode": {
        "settings": {
          "terminal.integrated.defaultProfile.linux": "bash"
        }
      }
    },
    "postCreateCommand": "containerlab version && ansible --version",
    "remoteUser": "devuser"
  }
  